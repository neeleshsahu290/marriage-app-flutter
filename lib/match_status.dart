// ignore_for_file: constant_identifier_names

enum MatchStatus {
  SENT,
  ACCEPTED,
  REJECTED,
  BLOCKED,
  RECOMMENDED,
  PASS,
  RECIEVED,
}

enum CardType { recieved, accepted, sent, recommend }
